import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.dabliuweb.pokemon',
  appName: 'pokemonApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
